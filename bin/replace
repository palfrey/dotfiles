#!/usr/bin/python
import sys

if len(sys.argv)<4:
	print "Usage: %s <from> <to> <files>"%sys.argv[0]
	exit(1)

for x in sys.argv[3:]:
	data = file(x).read()
	newdata = data.replace(sys.argv[1],sys.argv[2])
	if data!=newdata:
		print x,sys.argv[1:3]
		file(x,"w").write(newdata)
